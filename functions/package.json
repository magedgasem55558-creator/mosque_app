{
  "name": "functions",
  "description": "Cloud Functions for mosque app",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^11.5.0",
    "firebase-functions": "^4.2.0"
  },
  "engines": {
    "node": "22"
  }
}